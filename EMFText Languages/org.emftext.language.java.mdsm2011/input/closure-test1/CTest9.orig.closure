package test1;

import java.util.ArrayList;
import java.util.List;

public class CTest1 {

	public void m() {
		Runnable r = new Runnable() {
			
			public String run(String name, int x) {
				return "Hello Closure with multiple parameters " + name + ", " + x + " and return type";
			}
		};
		// don't invoke the overriden method and let's see if the it will be generated in the closure
		// invokaction of run() should NOT be generated in closure 
	}
}
